'use client'

interface Params {
    id: string;
}

export default function Page({ params }: { params: Params }) {
    const { id } = params;

    return (
        <>
            <h1> Page {id}</h1>
            <p> Page content</p>
        </>
    );
}